version: '3.8'

services:
    web:
        build: .
        ports:
            - "5000"
    nginx: 
        image: nginx:alpine
        ports:
            - "80:80"
    
    volumes:
        - ./static:/usr/share/nginx/html
        - ./nginx/nginx.conf:/etc/nginx/nginx.conf